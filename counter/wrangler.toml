name = "camera-counter"
main = "worker.js"
compatibility_date = "2024-09-01"

# KV 命名空間：執行 `wrangler kv namespace create COUNTER` 後，把回傳的 id 填到下面
[[kv_namespaces]]
binding = "COUNTER"
id = "REPLACE_WITH_KV_ID"
